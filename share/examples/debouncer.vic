PIC P16F690;

config debounce count = 5;
config debounce delay = 1ms;

Main {
    output_port 'C';
    digital_input_port 'A', 3;
    $display = 0;
    Loop {
        debounce 'A', 3, Action {
            $display++;
            port_value 'C', 0xFF, $display;
        };
    }
}
